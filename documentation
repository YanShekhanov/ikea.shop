    def parse_categories():
Функция парсит все категории, подкатегории и под подкатегории, записывает в базу title(названи), url_ikea(ссылка на сайте ikea.com/pl/pl),
priority(приоритет в парсинге, задается опционально в админке),is_translated(наличие перевода),
have_sub_subcategory(наличие под подкатегории у подкатегории), unique_identificator(уникальный идентификатор)

